name: .NET API Tests & Build

on:
  push:
    branches: [ Prod, Dev ]
  pull_request:
    branches: [ Prod, Dev ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        dotnet-version: ['10.0.x']
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET ${{ matrix.dotnet-version }}
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ matrix.dotnet-version }}
    
    - name: Restore dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore --configuration Release
    
    - name: Run Unit Tests
      run: dotnet test TableMasterApi.Tests/TableMasterApi.Tests.csproj --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx"
    
    - name: Upload Test Results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: '**/test-results.trx'
    
    - name: Publish API
      run: dotnet publish TableMasterApi/TableMasterApi.csproj -c Release -o ./publish
    
    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: api-build
        path: ./publish/
    
    - name: Test Report
      if: always()
      uses: dorny/test-reporter@v1
      with:
        name: .NET Test Results
        path: '**/test-results.trx'
        reporter: 'dotnet-trx'
